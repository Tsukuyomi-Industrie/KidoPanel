/**
 * Point d’entrée futur de la passerelle (`apps/gateway`).
 * Le frontend ne doit parler qu’à ce service, qui relaie vers les backends (voir `.cursorrules`).
 * Implémentation HTTP à ajouter (ex. routes vers `container-engine` et autres services).
 */
export {};
