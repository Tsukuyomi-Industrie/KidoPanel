/**
 * Préfixe d’URL géré par le serveur de dev Vite ; les requêtes sont relayées vers la passerelle
 * (voir `vite.config.ts`, `rewrite` vers la racine des routes Hono).
 */
export const CHEMIN_PROXY_PASSERELLE_DEV = "/__kidopanel_gateway";
