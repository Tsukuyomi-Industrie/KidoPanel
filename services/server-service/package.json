{
  "name": "server-service",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Service de gestion des instances « serveur » côté panel (sans Docker direct)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "node -e \"\""
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^6.0.2"
  }
}
